# GitHub Actions Workflow - Checklist ก่อน Push

## ✅ การตรวจสอบ Workflow

### **สถานะปัจจุบัน:**

#### **1. ไฟล์ Workflow**
- ✅ **ตำแหน่ง:** `.github/workflows/playwright-allure.yml.example`
- ⚠️ **ต้องเปลี่ยนชื่อ** เป็น `.yml` เพื่อเปิดใช้งาน

#### **2. Trigger เมื่อไหร่?**
```yaml
on:
  push:
    branches: [ main, develop ]  ✅ รันเมื่อ push ไป main/develop
  pull_request:
    branches: [ main, develop ]  ✅ รันเมื่อสร้าง PR
  schedule:
    - cron: '0 2 * * *'          ✅ รันทุกวัน 9:00 เช้า (เวลาไทย)
  workflow_dispatch:             ✅ สามารถรันด้วยมือได้
```

#### **3. NPM Scripts ที่ Workflow ใช้**
- ✅ `npm run test:ci` - มีใน package.json แล้ว
- ✅ `npm run allure:generate` - มีแล้ว
- ✅ `npm ci` - คำสั่งมาตรฐาน

#### **4. Dependencies**
- ✅ Node.js 18 - ตรงกับที่โปรเจคใช้
- ✅ Playwright - จะติดตั้งอัตโนมัติ
- ✅ Allure - ติดตั้งผ่าน npm

---

## 🎯 ตอนนี้ Workflow จะทำอะไร?

### **เมื่อคุณ Push ไป `main`:**

```
1. ⏳ Checkout code (2s)
2. ⏳ Setup Node.js 18 (5s)
3. ⏳ npm ci - ติดตั้ง dependencies (30-60s)
4. ⏳ ติดตั้ง Playwright browsers (1-2m)
5. ⏳ รัน tests (npm run test:ci) (1-2m)
   └─ ผลลัพธ์: 26 tests
6. ⏳ สร้าง Allure Report (10-15s)
7. ⏳ Upload Artifacts (screenshots, videos, reports) (20-30s)
8. ⏳ Deploy Report ไป GitHub Pages (15-20s)

🕐 รวมเวลา: ประมาณ 4-6 นาที
```

---

## ⚠️ สิ่งที่ต้องทำก่อน Push

### **1. เปิดใช้งาน Workflow (ตัวเลือก)**

**ถ้าต้องการให้ CI/CD ทำงานทันที:**

```bash
# Rename ไฟล์
mv .github/workflows/playwright-allure.yml.example .github/workflows/playwright-allure.yml

# Commit
git add .github/workflows/playwright-allure.yml
git commit -m "ci: enable GitHub Actions workflow"
```

**ถ้าไม่ต้องการตอนนี้:**
- ปล่อยเป็น `.example` ไว้
- เปิดใช้งานทีหลังได้

---

### **2. ตั้งค่า GitHub Pages (สำหรับ Allure Report)**

**ทำหลัง push แล้ว:**

```
1. GitHub → Settings → Pages
2. Source: เลือก "gh-pages" branch
3. Save
```

**URL Report:**
```
https://YOUR_USERNAME.github.io/YOUR_REPO/allure-report-1/
```

---

### **3. ตรวจสอบ Branch Name**

Workflow รันเฉพาะ branch:
- ✅ `main`
- ✅ `develop`

**ตอนนี้ branch คุณคือ:**
```bash
git branch
# * main  ← ถูกต้อง! จะรัน workflow
```

**ถ้าเป็น `master`:**
```yaml
# แก้ใน workflow file
on:
  push:
    branches: [ master, develop ]  # เปลี่ยนจาก main เป็น master
```

---

## 📊 สิ่งที่จะเกิดขึ้นเมื่อ Push ครั้งแรก

### **สถานการณ์ที่ 1: ไฟล์ยังเป็น `.example`**

```
✅ Push สำเร็จ
✅ Code ขึ้น GitHub แล้ว
❌ Workflow ไม่รัน (เพราะชื่อไฟล์ยัง .example)

→ ไม่มี CI/CD ทำงาน
→ ไม่มี Allure Report อัตโนมัติ
```

**ดีไหม:** ดี! ถ้าคุณยังไม่พร้อม

---

### **สถานการณ์ที่ 2: Rename เป็น `.yml` แล้ว**

```
✅ Push สำเร็จ
✅ Code ขึ้น GitHub แล้ว
✅ Workflow เริ่มรัน! 🚀

GitHub Actions:
├─ ⏳ กำลังรัน tests...
├─ ⏳ กำลังสร้าง Allure report...
└─ ⏳ กำลัง deploy report...

4-6 นาทีต่อมา:
✅ เสร็จสมบูรณ์!
📊 Allure Report พร้อมดู
```

**ดูที่:** GitHub → Actions tab

---

## 🔍 ตรวจสอบ Workflow

### **1. ดู Workflow บน GitHub**

```
1. Push code ขึ้น GitHub
2. GitHub → Actions tab
3. เห็น workflow ชื่อ "Playwright Tests with Allure Report"
4. คลิกเข้าไปดูรายละเอียด
```

### **2. ดู Logs แต่ละ Step**

```
Actions → เลือก workflow run → คลิก "Run Playwright Tests"

จะเห็น:
✅ Checkout code
✅ Setup Node.js
✅ Install dependencies
✅ Install Playwright
⏳ Run tests ← กำลังรันอยู่
```

### **3. ดู Test Results**

```
Scroll ลงมาในหน้า workflow run

Artifacts:
📦 allure-results (ข้อมูลดิบ)
📦 allure-report (HTML report)
📦 test-artifacts (screenshots, videos)

→ คลิก Download ได้เลย
```

---

## 🚨 สิ่งที่อาจเกิดปัญหา (และวิธีแก้)

### **ปัญหา 1: Workflow Fail ตอนติดตั้ง**

**สาเหตุ:**
- Node version ไม่ตรง
- Dependencies มีปัญหา

**แก้:**
```yaml
# ใน workflow file
- name: Setup Node.js
  uses: actions/setup-node@v4
  with:
    node-version: '18'  # ตรวจสอบว่าตรงกับ package.json
```

---

### **ปัญหา 2: Tests Fail**

**สาเหตุ:**
- Test ผิดพลาด (ถูกต้อง - เป็นการเจอ bug!)
- Environment แตกต่างจาก local

**ดู:**
```
Actions → workflow run → ดู test logs
→ Allure Report จะแสดงว่า test ไหน fail
```

**แก้:**
```bash
# แก้ test ในเครื่อง
# ... fix code ...

# Commit และ push ใหม่
git add .
git commit -m "fix: resolve failing test"
git push

# Workflow จะรันใหม่อัตโนมัติ
```

---

### **ปัญหา 3: GitHub Pages ไม่แสดง Report**

**สาเหตุ:**
- ยังไม่เปิด GitHub Pages
- Branch `gh-pages` ยังไม่มี

**แก้:**
```
1. รอ workflow รันเสร็จครั้งแรก
2. Settings → Pages
3. Source: gh-pages
4. Save
5. รอ 2-3 นาที
```

---

## 📝 Checklist ก่อน Push

```
□ Code รัน test ผ่านในเครื่อง (npm test)
□ Allure report สร้างได้ (npm run test:allure)
□ ตัดสินใจว่าจะเปิด workflow หรือยัง
   □ เปิดเลย → rename เป็น .yml
   □ ยังก่อน → ปล่อยเป็น .example
□ Branch name ตรงกับ workflow (main หรือ develop)
□ ไม่มี sensitive data (password, API keys)
□ .gitignore ครบถ้วน (node_modules, .env)
```

---

## 🎯 แนะนำสำหรับ Push ครั้งแรก

### **วิธีที่ 1: ปลอดภัย (แนะนำ)**

```bash
# Push โดยยังไม่เปิด workflow
git push origin main

# ดู code บน GitHub ก่อน
# ตรวจสอบว่าทุกอย่างถูกต้อง

# แล้วค่อยเปิด workflow ทีหลัง
mv .github/workflows/playwright-allure.yml.example .github/workflows/playwright-allure.yml
git add .
git commit -m "ci: enable GitHub Actions"
git push
```

**ข้อดี:**
- ✅ ปลอดภัย เช็คได้ก่อน
- ✅ ไม่เสียเวลา (ถ้ามีอะไรผิด)

---

### **วิธีที่ 2: เปิดเลย**

```bash
# Rename workflow file
mv .github/workflows/playwright-allure.yml.example .github/workflows/playwright-allure.yml

# Commit ทั้งหมด
git add .
git commit -m "ci: enable GitHub Actions and push initial code"

# Push
git push origin main

# Workflow จะรันทันที! 🚀
```

**ข้อดี:**
- ✅ ได้ Allure report ทันที
- ✅ เห็นผล CI/CD เลย

---

## 🎉 สรุป

### **Workflow พร้อมใช้งาน!**

**ตอนนี้:**
- ✅ Workflow ถูกต้อง
- ✅ NPM scripts ครบ
- ✅ Dependencies พร้อม
- ✅ พร้อม push!

**เลือก:**
1. **Push ก่อน, เปิด workflow ทีหลัง** (ปลอดภัย)
2. **Push พร้อมเปิด workflow** (ได้ CI/CD ทันที)

**คำแนะนำ:**
```bash
# สำหรับครั้งแรก แนะนำ:
1. Push โดยไม่เปิด workflow
2. เช็คว่า code ขึ้น GitHub ถูกต้อง
3. เปิด GitHub Pages
4. แล้วค่อยเปิด workflow

# การ push ครั้งต่อไปจะมี CI/CD ทำงานทุกครั้ง!
```

---

## 📚 เอกสารเพิ่มเติม

- **คำอธิบาย Workflow แบบละเอียด:** [CI_CD_THAI_EXPLANATION.md](./CI_CD_THAI_EXPLANATION.md)
- **คู่มือเร่งด่วน:** [CI_CD_QUICK_GUIDE_TH.md](./CI_CD_QUICK_GUIDE_TH.md)

**พร้อม Push แล้วค่ะ! 🚀**
